PRJ_NAME=mxvOMP

GCC=g++

OUTPUT=${PRJ_NAME}.exe
SOURCES=${PRJ_NAME}.cpp
CCFLAGS= -g3 -O0

all: ${OUTPUT}
${OUTPUT}:
	${GCC} -o ${OUTPUT} ${CCFLAGS} ${SOURCES}
    
clean:
	rm -f ${OUTPUT}
    
